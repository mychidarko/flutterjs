import 'package:flutter/material.dart';
import 'package:flutterjs/screens/login/loginForm.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: LoginForm(),
      ),
    );
  }
}
